#include <iostream>



int main() {
    std::cout << "Hello, World!" << std::endl;
    //run(std::cin, std::cout);

    //start(std::cin, std::cout);

    // memoryLost(arr_size);
    // extraIndex(arr_size);
    return 0;
}